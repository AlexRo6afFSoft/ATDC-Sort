#include "atdc_sort.c"
#include <stdio.h>
#include <stdlib.h>
#include <time.h>

int main()
{
	/////////////////////////////////////////////////////////////////////
	/// Exmaple for use:
	/////////////////////////////////////////////////////////////////////
	srand(time(0));
	int arr[100];
	for(int i = 0 ; i < 100; i++)
	{
		arr[i] = rand();
		printf("%d, ", arr[i]);
	}
	printf("\n\n------------------------------------------------------------------------------------------------------------\n\n\n");
	/////////////////////////////////////////////////////////////////////
	/// Use atdc sort
	/////////////////////////////////////////////////////////////////////

//	atdc_sort (arr, arr + i);
	
	for(int i = 0 ; i < 100 ; i ++)
	{
		printf("%d, ", arr[i]);
	}
	return 0;
}
